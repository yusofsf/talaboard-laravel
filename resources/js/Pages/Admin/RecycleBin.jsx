import { router } from '@inertiajs/react'; import AppLayout from '../../Layouts/AppLayout';
export default function RecycleBin({items}) { return <AppLayout title="حذف‌شده‌ها"><main className="container" style={{maxWidth:960}}><div className="page-head"><div><h1>حذف‌شده‌ها</h1><p>رکوردهای حذف نرم را بازگردانی کنید.</p></div></div><div className="card"><div className="table-wrap"><table><thead><tr><th>نوع</th><th>رکورد</th><th>زمان حذف</th><th>عملیات</th></tr></thead><tbody>{items.map(i=><tr key={`${i.type}-${i.id}`}><td dir="ltr">{i.type}</td><td>{i.label}</td><td dir="ltr">{i.deleted_at}</td><td><button className="btn-sm" onClick={()=>router.post(`/admin/recycle-bin/${i.type}/${i.id}/restore`)}>بازگردانی</button></td></tr>)}{!items.length&&<tr><td colSpan="4">رکوردی وجود ندارد.</td></tr>}</tbody></table></div></div></main></AppLayout> }
